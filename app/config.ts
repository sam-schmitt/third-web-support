const LLM_URL = "wss://hypha-be-develop.azurewebsites.net/ws/chat"; // endpoint for our LLM that returns transaction data
const THIRDWEB_CLIENT_ID = ""; // enter your thirdweb client id

export const config = {
	LLM_URL,
	THIRDWEB_CLIENT_ID,
};
